import { notFound } from "next/navigation";import { prisma } from "@/lib/prisma";import { saveRoomAction } from "@/lib/actions";export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const r=await prisma.room.findUnique({where:{id}});if(!r)notFound();return <><h1 className="text-3xl font-bold">Изменить номер</h1><form action={saveRoomAction} className="card mt-6 grid gap-4 max-w-3xl"><input type="hidden" name="id" value={r.id}/><label>Название<input className="field mt-2" name="name" defaultValue={r.name} required/></label><div className="grid gap-4 sm:grid-cols-2"><label>Код<input className="field mt-2" name="code" defaultValue={r.code} required/></label><label>Адрес страницы<input className="field mt-2" name="slug" defaultValue={r.slug} required/></label></div><label>Описание<textarea className="field mt-2 min-h-28" name="description" defaultValue={r.description}/></label><label>Правила<textarea className="field mt-2" name="rules" defaultValue={r.rules||""}/></label><div className="grid gap-4 sm:grid-cols-4"><label>Цена, ₽<input className="field mt-2" name="price" type="number" defaultValue={r.basePriceMinor/100}/></label><label>Вместимость<input className="field mt-2" name="capacity" type="number" defaultValue={r.capacity}/></label><label>Мин. ночей<input className="field mt-2" name="minNights" type="number" defaultValue={r.minNights}/></label><label>Макс. ночей<input className="field mt-2" name="maxNights" type="number" defaultValue={r.maxNights}/></label></div><label className="font-normal"><input type="checkbox" name="isActive" defaultChecked={r.isActive}/> Доступен</label><label className="font-normal"><input type="checkbox" name="isRecommended" defaultChecked={r.isRecommended}/> Рекомендуемый</label><button className="btn">Сохранить</button></form></>}
