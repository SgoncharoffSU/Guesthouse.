import Link from "next/link";import { prisma } from "@/lib/prisma";import { archiveRoomAction,addImageAction } from "@/lib/actions";import { formatMoney } from "@/lib/domain";
export default async function RoomsAdmin(){const rooms=await prisma.room.findMany({where:{archivedAt:null},include:{images:true},orderBy:{name:"asc"}});return <><div className="flex items-center justify-between"><h1 className="text-3xl font-bold">Номера</h1><Link href="/admin/rooms/new" className="btn">Добавить номер</Link></div><div className="mt-6 grid gap-5 lg:grid-cols-2">{rooms.map(r=><article className="card" key={r.id}><div className="flex gap-4">{r.images[0]?<img src={r.images[0].url} alt="" className="h-28 w-36 rounded-xl object-cover"/>:<div className="h-28 w-36 rounded-xl bg-slate-200"/>}<div><h2 className="text-xl font-bold">{r.name}</h2><p>{r.code} · {formatMoney(r.basePriceMinor)} · до {r.capacity} гостей</p><p className={r.isActive?"font-bold text-green-700":"font-bold text-red-700"}>{r.isActive?"Доступен":"Скрыт"}</p></div></div><div className="mt-4 flex flex-wrap gap-3"><Link href={`/admin/rooms/${r.id}`} className="btn">Изменить</Link><form action={archiveRoomAction}><input type="hidden" name="id" value={r.id}/><button className="btn btn-secondary">Архивировать</button></form></div><form action={addImageAction} className="mt-4 grid gap-2 border-t pt-4"><input type="hidden" name="roomId" value={r.id}/><input className="field" name="url" placeholder="URL фотографии" required/><input className="field" name="alt" placeholder="Подпись"/><label className="font-normal"><input type="checkbox" name="isPrimary"/> Главная фотография</label><button className="btn btn-secondary">Добавить фото</button></form></article>)}</div></>}
