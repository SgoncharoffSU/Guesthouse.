import Link from "next/link";import { prisma } from "@/lib/prisma";
export default async function Bookings({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams;const search=q.search||"";const data=await prisma.booking.findMany({where:search?{OR:[{number:{contains:search,mode:"insensitive"}},{guestName:{contains:search,mode:"insensitive"}},{guestPhone:{contains:search}},{room:{name:{contains:search,mode:"insensitive"}}}]}:{},include:{room:true},orderBy:{createdAt:"desc"},take:100});return <><h1 className="text-3xl font-bold">Бронирования</h1><form className="card mt-6 flex gap-3"><input className="field" name="search" defaultValue={search} placeholder="Имя, телефон, номер брони или комнаты"/><button className="btn">Найти</button></form><div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[800px]"><thead><tr className="text-left"><th className="p-3">Бронь</th><th>Гость</th><th>Номер</th><th>Даты</th><th>Статус</th><th></th></tr></thead><tbody>{data.map(b=><tr className="border-t" key={b.id}><td className="p-3">{b.number}</td><td>{b.guestName}<br/>{b.guestPhone}</td><td>{b.room.name}</td><td>{b.checkIn.toLocaleDateString("ru-RU")} — {b.checkOut.toLocaleDateString("ru-RU")}</td><td>{b.status}</td><td><Link className="btn btn-secondary" href={`/admin/bookings/${b.id}`}>Открыть</Link></td></tr>)}</tbody></table></div></>}
