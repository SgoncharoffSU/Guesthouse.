import { prisma } from "@/lib/prisma";export const dynamic="force-dynamic";export default async function Rules(){const s=await prisma.appSettings.upsert({where:{id:"singleton"},update:{},create:{}});return <main className="mx-auto max-w-3xl px-5 py-10"><h1 className="text-3xl font-bold">Правила проживания, оплаты и отмены</h1><div className="card mt-6 space-y-5"><p><b>Заезд:</b> {s.checkInTime}. <b>Выезд:</b> {s.checkOutTime}.</p><section><h2 className="text-xl font-bold">Отмена</h2><p className="mt-2 whitespace-pre-wrap">{s.cancellationRules||"Правила пока не опубликованы."}</p></section><section><h2 className="text-xl font-bold">Возврат</h2><p className="mt-2 whitespace-pre-wrap">{s.refundRules||"Правила пока не опубликованы."}</p></section></div></main>}
