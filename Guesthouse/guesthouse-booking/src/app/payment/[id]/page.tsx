import { notFound } from "next/navigation";import { prisma } from "@/lib/prisma";import { formatMoney } from "@/lib/domain";import { completeTestPaymentAction } from "@/lib/actions";
export const dynamic="force-dynamic";
export default async function Payment({params}:{params:Promise<{id:string}>}){const {id}=await params;const b=await prisma.booking.findUnique({where:{id},include:{room:true}});if(!b)notFound();return <main className="mx-auto max-w-xl px-5 py-12"><div className="card"><h1 className="text-3xl font-bold">Предоплата</h1><p className="mt-4">{b.room.name}, {b.checkIn.toLocaleDateString("ru-RU")} — {b.checkOut.toLocaleDateString("ru-RU")}</p><p className="mt-3 text-2xl font-bold">К оплате: {formatMoney(b.prepaymentMinor)}</p><p className="mt-3 text-slate-600">Бронь удерживается до {b.holdExpiresAt?.toLocaleTimeString("ru-RU")}. В production тестовая оплата автоматически отключена.</p>{b.status==="AWAITING_PAYMENT"?<form action={completeTestPaymentAction} className="mt-6"><input type="hidden" name="bookingId" value={id}/><button className="btn w-full">Оплатить в тестовом режиме</button></form>:<p className="mt-6 rounded-xl bg-green-50 p-4 font-bold">Статус бронирования: {b.status}</p>}</div></main>}
