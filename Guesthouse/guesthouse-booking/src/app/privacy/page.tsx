import { prisma } from "@/lib/prisma";export const dynamic="force-dynamic";export default async function Privacy(){const s=await prisma.appSettings.upsert({where:{id:"singleton"},update:{},create:{}});return <main className="mx-auto max-w-3xl px-5 py-10"><h1 className="text-3xl font-bold">Политика конфиденциальности</h1><div className="card mt-6 whitespace-pre-wrap">{s.privacyPolicy||"Политика пока не опубликована."}</div><h2 className="mt-8 text-2xl font-bold">Согласие на обработку данных</h2><div className="card mt-4 whitespace-pre-wrap">{s.personalDataConsent||"Текст согласия пока не опубликован."}</div></main>}
