import { loginAction, registerAction } from "@/lib/actions";
export default async function Login({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams;return <main className="mx-auto grid max-w-4xl gap-6 px-5 py-12 md:grid-cols-2"><form action={loginAction} className="card grid gap-4"><h1 className="text-3xl font-bold">Вход</h1>{q.error&&<p className="rounded-lg bg-red-50 p-3 text-red-800">Неверный телефон или пароль.</p>}<label>Телефон<input className="field mt-2" name="phone" type="tel" required/></label><label>Пароль<input className="field mt-2" name="password" type="password" required/></label><button className="btn">Войти</button></form><form action={registerAction} className="card grid gap-4"><h2 className="text-3xl font-bold">Регистрация</h2>{q.registerError&&<p className="rounded-lg bg-red-50 p-3 text-red-800">Проверьте данные. Пароль — не менее 8 символов.</p>}<label>Имя<input className="field mt-2" name="name" required/></label><label>Телефон<input className="field mt-2" name="phone" type="tel" required/></label><label>Пароль<input className="field mt-2" name="password" type="password" minLength={8} required/></label><button className="btn">Создать аккаунт</button></form></main>}
